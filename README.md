in dev now...

# .gitignore generator
type some keywords to generate a .gitignore file.

# Usage

```sh
ignore [keyword [keyword]] ...
```

eg.

```
ignore mac node
```
will generate these:

```
.DS_Store
node_modules/*
```